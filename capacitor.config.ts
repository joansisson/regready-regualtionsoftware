import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.regready.localpro',
  appName: 'RegReady Local Pro',
  webDir: 'dist/public'
};

export default config;
